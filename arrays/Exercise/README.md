# 🏁 Exercise 1: Reverse an Array In‑Place

## 🎯 Goal: Write a function that takes an array and reverses its elements without using extra arrays (i.e. O(1) extra space).

###  --ex1-drill- 🥋 Pair Sum Drill — “Two Pointers Technique”
* 💡 Problem Statement
  Given a sorted array of integers and a target value, return true if any two numbers in the array sum up to the target.
    Otherwise, return false.

# 🧪 Exercise 2: Max Sum of Subarray (Sliding Window)
##  Goal:

- Write a function that takes an array and a window size k, and finds the maximum sum of any subarray of length k, using the Sliding Window technique.
## drill is in ex2


#  ✅ Exercise 3: Max Subarray Sum (No Fixed Size) — Kadane’s Algorithm
## Goal:

- Write a function that finds the maximum sum of any contiguous subarray (no fixed window size k).
Use Kadane’s Algorithm, a greedy pattern that adjusts window size dynamically based on value trends.
# 💡 Real-World Scenario: Energy Grid Monitoring

* You're analyzing a list of power grid readings, which represent net energy balance per minute.

* Each reading:

    > 0: Energy surplus,  < 0: Energy deficit

- Your task is to find the healthiest contiguous streak where the net sum was the highest, meaning the grid was at peak operational stability.